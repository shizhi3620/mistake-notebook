# 01 — 省份单列选择与可选建档

**What to build:** 用户新建或编辑孩子档案时，只能选择省份，且省份不是必填项；保存后的档案可以正常读取和继续编辑。

**Blocked by:** None — can start immediately

**Status:** resolved

- [x] 新建和编辑孩子档案使用单列省份选择器，不出现市或区县选择列。
- [x] 省份可选，省份为空时仍可创建或更新档案。
- [x] 新版请求提交省份代码和名称，后端可保存并恢复仅省份档案。
- [x] 领域、HTTP、存储测试覆盖省份为空、合法省份和非法省份输入。

## Answer

已完成。小程序使用省份单列 picker，省份可跳过；领域和 SQLite 存储支持仅省份档案及格式校验，HTTP 继续传递该结构。
