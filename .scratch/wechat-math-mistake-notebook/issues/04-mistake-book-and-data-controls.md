# 04: 错题本浏览、检索与数据控制

**What to build:** 家长能够浏览孩子已确认的错题、按学习维度找到需要处理的题，并对重复记录和孩子学习数据行使控制权。

**Blocked by:** 03: 适龄讲解与确认错题.

**Status:** resolved

- [x] 已保存错题及时出现在对应孩子的错题本中，并能展示题目、知识点、错因、日期和掌握状态。
- [x] 家长可组合使用知识点、错因、日期、掌握状态和题干关键词筛选与检索。
- [x] 系统可提示疑似重复题，但由家长决定是否合并；不会自动删除学习记录。
- [x] 家长可删除单题或整个孩子档案，关联图片、讲解与学习数据一并删除，且随后无法从旧入口读取。

## Comments

- 2026-08-26: 领域层完成（`test/mistake-book.test.ts` 5 个测试通过）。`listMistakes` 返回题目、知识点、错因、日期与掌握状态（默认 `not-started`，由复习闭环更新），支持知识点（主/次，子串）、错因、日期区间、掌握状态与题干关键词组合筛选。`findDuplicateMistakes` 仅按归一化题干提示疑似重复分组，不删除任何记录；`mergeMistakes` 由家长指定保留方，合并次知识点与错因后移除另一方。`deleteMistake`/`deleteChildProfile`/`deleteParentAccount`（对应用户故事 37 的全部数据删除）级联清除题目、错题、草稿、凭证与会话，旧入口随后一律不可读；SQLite 级联包在事务中。图片二进制不在领域层，删除即解除 imageKey 引用。

## Answer

已完成并由错题检索、合并、账户隔离和删除测试覆盖。
